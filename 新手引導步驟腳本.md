# Code Dungeon — 新手引導步驟腳本（v1 草案）

> 這是**設計文件,不是程式碼**。對應 Todoist「引導 — 設計互動式逐步引導的步驟腳本」。
> 目的:在寫任何引導程式之前,先把每一步的觸發條件、提示文字、完成判定想清楚。
>
> **關於「目標」**:UI 上目前沒有常駐的目標區塊(每關 hint 只出現在開場 log 與結算畫面)。
> 本版**不新增 UI 元件**,改用引導氣泡的文字把目標講清楚(見 Step 2)。
> 若日後實測發現玩家中途忘記目標,再考慮加常駐區塊——但那是後續決策,不在這一版。

---

## 設計原則

1. **一步只教一件事。** 每步只高亮一個元素、只講一句重點。
2. **教到「能動」為止就放手。** 引導的目標是讓玩家看到勇者動起來,不是教會 JavaScript。
3. **由行動推進,不由「下一步」鈕推進。** 玩家真的做了那件事才前進(除非該步純粹是說明)。
4. **全程可跳過。** 右上角常駐「跳過教學」;跳過後在工具列留「? 重看教學」入口。
5. **不擋畫面。** 氣泡出現在被高亮元素的旁邊,不覆蓋地圖與編輯器。
6. **教學只在 L1 跑一次。** 存 localStorage(同 key `codeDungeonProtoV2`,新增欄位 `tutorial`)。

---

## 主線:第 1 關開場(Step 1–6)

這 6 步是核心,對應觀察清單第 2 節「最初 5 分鐘」。目標是讓玩家在 **3 分鐘內看到勇者動起來**。

### Step 1 — 這是地圖

| 項目 | 內容 |
|---|---|
| 觸發 | 進入 L1 且 `tutorial` 未完成 |
| 高亮 | `#mapwrap` + `.legend` |
| 文字 | 這裡是地牢。綠色是你的勇者,橘色是敵人,藍色是往下一層的樓梯。<br>暗色的地方是勇者還沒看到的區域——**你看得到,但勇者看不到**。 |
| 完成 | 玩家按「知道了」 |
| 可跳過 | 是 |

> 備註:最後一句是資訊差設計的核心,一定要在第一步就講,否則玩家之後不會理解為什麼 `getEnemies()` 拿不到東西。

### Step 2 — 你的目標

| 項目 | 內容 |
|---|---|
| 觸發 | Step 1 完成 |
| 高亮 | 地圖上的樓梯格(藍色那格)。若技術上不好單獨高亮,退而高亮 `.legend` 中的「樓梯」項 |
| 文字 | **目標:讓勇者走到樓梯。**<br>這一關的路上有兩隻敵人擋著,得先解決牠們。 |
| 完成 | 玩家按「知道了」 |
| 可跳過 | 是 |

> 備註:這步就是「目標」區塊的替代方案。L1 地圖是一條直走廊(`#h..s....g>#`),敵人擋在中間,講「先解決牠們」不會誤導。

### Step 3 — 但你不能直接操控他

| 項目 | 內容 |
|---|---|
| 觸發 | Step 2 完成 |
| 高亮 | `#editor` |
| 文字 | 你不能用鍵盤直接控制勇者。<br>你要**寫一段程式,決定他每一步怎麼判斷**。<br>這裡已經幫你放了一份能用的腳本,先不用改。 |
| 完成 | 玩家按「知道了」 |
| 可跳過 | 是 |

> 備註:這一步是整個遊戲最需要建立的心智模型。「你寫的是判斷邏輯,不是操作指令」——這句話決定玩家之後會不會嘗試寫 `move("right"); move("right"); move("right")` 這種死路。

### Step 4 — 按這裡讓他動

| 項目 | 內容 |
|---|---|
| 觸發 | Step 3 完成 |
| 高亮 | `#runbtn` |
| 文字 | 按【▶ 執行】,勇者就會照你的腳本行動。 |
| 完成 | **玩家實際按下 `#runbtn`**(不接受「知道了」) |
| 可跳過 | 是(但跳過後仍在按鈕旁留一個持續的箭頭,直到玩家按過一次) |

> 備註:這是最容易卡住的地方(觀察清單第 2 節必查項)。必須由真實點擊推進,不能讓玩家按「知道了」矇混過去。

### Step 5 — 它會一直重跑

| 項目 | 內容 |
|---|---|
| 觸發 | 腳本執行完第一輪(`state.turn` 首次超過腳本一輪的行動數),且勇者仍存活 |
| 高亮 | `#editor` 頂端 |
| 文字 | 注意看:腳本從頭又跑了一次。<br>**腳本跑完會自動重來,直到你死掉或通關。**<br>所以你不需要自己寫 `while` 迴圈,只要寫「這一步該做什麼」就好。 |
| 完成 | 玩家按「知道了」(執行不暫停,讓他邊看邊讀) |
| 可跳過 | 是 |

> 備註:**這是整份引導最重要的一步。** TFWR 模式最反直覺的地方,不教會的話玩家會寫出巢狀迴圈然後困惑。
> 時機必須是「他親眼看到重跑」的當下,提前講沒有用。

### Step 6 — 這裡會告訴你發生什麼事

| 項目 | 內容 |
|---|---|
| 觸發 | Step 5 完成,或勇者第一次受傷(以先發生者為準) |
| 高亮 | `#log` |
| 文字 | 每一回合發生的事都會記在這裡。<br>失敗的時候,回來看這裡通常找得到原因。 |
| 完成 | 玩家按「知道了」 |
| 可跳過 | 是 |

**→ 主線結束。** 此時玩家應該已經在看勇者自己打完 L1。剩下的功能改為「情境觸發」,不再連續打斷。

---

## 情境觸發(Step 7 起,非連續)

這些不在開場一次講完,而是**在玩家真的需要時才出現一次**。每個只出現一次,出現後記進 `tutorial.seen[]`。

### Step 7 — HP 與藥水

| 項目 | 內容 |
|---|---|
| 觸發 | 勇者 HP 首次低於 70%(35/50) |
| 高亮 | `.hpwrap`(HP 條 + 藥水數) |
| 文字 | 勇者受傷了。HP 歸零就會失敗,關卡從頭開始。<br>地圖上黃色的是藥水,走過去會自動撿起來,用 `drinkPotion()` 喝掉回 30 HP。 |

### Step 8 — 慢動作看清楚

| 項目 | 內容 |
|---|---|
| 觸發 | 玩家**第二次**失敗於同一關 |
| 高亮 | `#speedbtn` `#pausebtn` `#stepbtn` |
| 文字 | 想看清楚每一步發生什麼?<br>調慢【速度】、或用【⏸ 暫停】和【⏭ 下一步】一格一格看。 |

> 備註:第一次失敗不要跳,那時玩家還在氣頭上。第二次才是他真的想 debug 的時刻。

### Step 9 — 出錯了可以重來

| 項目 | 內容 |
|---|---|
| 觸發 | 玩家改過編輯器內容(`#editor` 的 input 事件)且失敗過至少一次 |
| 高亮 | `#resetbtn` `#reseteditorbtn` |
| 文字 | 【↺ 重置關卡】把地圖恢復原狀,【⤺ 還原程式碼】把腳本還原成一開始的版本。<br>改壞了不用怕。 |

### Step 10 — 有哪些函式可以用

| 項目 | 內容 |
|---|---|
| 觸發 | 玩家通過 L1,進入 L2 時 |
| 高亮 | 「API 參考」的 `<summary>` |
| 文字 | 接下來的關卡光靠初始腳本過不了。<br>**點開這裡看有哪些函式可以用**——共 14 個,例如 `hp()`、`drinkPotion()`、`getItems()`。 |
| 額外 | 這步應該**自動把 `<details>` 展開一次**,讓玩家看到裡面有東西,而不只是知道有個可以點的東西 |

> 備註:觀察清單第 4 節問「怎麼知道有哪些函式可用?」。目前 API 參考是收合的,不主動指出來玩家很可能整場沒發現。

### Step 11 — par 與 ⭐

| 項目 | 內容 |
|---|---|
| 觸發 | 玩家**第一次通關但超過 par**(結算畫面顯示「比 par 多花 N 回合」時) |
| 高亮 | 結算畫面的 par 那一行 |
| 文字 | 過關了!不過每關都有一個目標回合數(par)。<br>用更少回合通關會拿到 ⭐——不強制,但那通常代表你的邏輯更聰明。 |

### Step 12 — 技能與修煉場

| 項目 | 內容 |
|---|---|
| 觸發 | 進入 L6 時(該關 hint 已明示需要 skill) |
| 高亮 | `#skillbar` |
| 文字 | 你一直在用的 `nearest()` 其實是個很笨的版本——它只會回傳清單裡的第一個,不管遠近。<br>點下面的技能自己實作一個更好的,**練成後闖關就會用你寫的版本**。 |

> 備註:這是全遊戲最有特色的系統,但也最容易被忽略。L6 的 hint 已經寫了引導文字,這步是把它從文字提示升級成視覺指引。
> 「你一直在用的是笨版本」這個說法是刻意的——製造「原來如此」的落差感比平鋪直敘「你可以練技能」有效。

---

## 實作技術筆記(已查證,2026-07-31)

以下四點在寫這份腳本時列為待決,已實際讀過 `Code Dungeon Prototype.html` 確認。**結論:全部可行,不需要改動現有架構。**

### 1. Step 5 的觸發點:現成的

`startRun()` 的主迴圈(約第 667 行)本來就有「一輪結束」的明確邊界:

```js
while (running && !stopFlag) {
  var before = state.turn;
  await program();          // ← 這行結束 = 腳本跑完一輪
  loopGuard = 0;
  if (state.turn === before) { addLog("腳本跑完一輪但沒有執行任何行動…"); break; }
}
```

在 `await program()` 之後加一個 iteration 計數器,**第 2 輪開始時**觸發 Step 5(此時玩家剛親眼看到重跑)。
不需要改動迴圈結構。注意 Step 5 觸發時**不要暫停執行**,讓玩家邊看勇者動邊讀提示。

### 2. 高亮地圖單一格:可行,但要疊在上層

**不能**把 highlight class 加在 `.cell` 上——`render()` 每回合用 `innerHTML` 整個重建 `#map`(約第 600 行),
任何寫進格子的狀態都會被沖掉。

正解是絕對定位疊層:

- `#mapwrap` 已經是 `position: relative`(CSS 第 21 行)
- `#map` 是固定 32px 的 CSS grid(`.cell` 為 32×32,CSS 第 22–23 行)
- 所以格子 `(x, y)` 的像素位置就是 `left: x*32px, top: y*32px`

在 `#mapwrap` 內插入一個 32×32 的絕對定位方框(例如描邊 + 呼吸動畫)即可。
這個做法**完全不碰 `render()`**,重繪也不會影響它。參考同層的 `#overlay`(`position:absolute; inset:0`,CSS 第 24 行)。

⇒ Step 2 高亮樓梯格照原案做,不需要退回高亮 legend。

### 3. 存檔:零改動

`save(patch)` 本來就是 shallow merge(第 319 行),`loadSave()` 回傳整個物件(第 316 行):

```js
save({ tutorial: { done: false, step: 3, seen: ["hp", "api"] } });
```

跟現有的 `cur` / `code` / `trials` / `levels` 並列在同一個 key(`codeDungeonProtoV2`)下,不需要改存檔邏輯。

建議結構:

```js
tutorial: {
  done: false,        // 主線 Step 1–6 是否走完(或被跳過)
  step: 0,            // 主線目前進度
  seen: []            // 情境觸發已出現過的 id,如 ["hp","slowmo","reset","api","par","skill"]
}
```

### 4.「重看教學」入口:放標題旁

工具列(`.topbar`)已經有 7 個按鈕 + HP + 藥水 + 關卡導覽 + checkbox,再加會太擠。
建議放在左上角 `<h1>Code Dungeon <span>prototype v0</span></h1>` 旁邊,做成一個小的 `?` 按鈕——
那裡有空間,而且不會跟操作類按鈕混在一起。

---

## 其他實作注意事項

- **零依賴**:引導層寫在同一個 HTML 檔內,維持 prototype「雙擊即玩」的特性(CLAUDE.md 慣例)。
- **不要用 `"use strict"`**:`test/headless.js` 靠 sloppy-mode direct eval 取得遊戲內部狀態。
- **改完必跑** `node test/headless.js`,確認 16 案例全過。引導層若碰到初始腳本或關卡資料尤其要確認。
- **文案語氣**:目前用「你/勇者」第二人稱、句子短。若日後主題改動,這批文案要一起檢查。
- **相關程式區塊**(檔案內已分區標註):
  - 1) 關卡資料 — `LEVELS`(約 156 行)
  - 9) 關卡載入 — `loadLevel()`(約 636 行)
  - 10) 執行控制 — `startRun()`(約 651 行)← Step 5 接點
  - `render()`(約 587 行)← 地圖重繪,勿在此加引導狀態
  - `addLog()`(約 576 行)、`save()`/`loadSave()`(約 315–325 行)

---

## 自我驗收(對應 Todoist「引導 — 驗收」)

- [ ] 清空 localStorage,以「假裝第一次玩」走一遍,不靠任何外部知識能通過 L1
- [ ] 全程沒有一步是「不知道現在該做什麼」
- [ ] 引導氣泡沒有擋住地圖或編輯器
- [ ] 按「跳過教學」後遊戲完全正常,且找得到重看入口
- [ ] 情境觸發的步驟(7–12)不會在錯誤時機跳出來
- [ ] 跑 `node test/headless.js`,16 案例全過
