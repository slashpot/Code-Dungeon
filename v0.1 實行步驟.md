# v0.1 實行步驟（v0.1-web，第一個正式版本）

> 2026-07-24 初版（Godot）；2026-07-31 因路線翻案（Godot→Web）全文改寫。
> 目標內容不變：基礎故事、基礎視覺、地圖可行走、一關 dungeon。
> 平台：HTML+JS——**Pixi.js**（渲染）＋ **Worker/JS-Interpreter/Babel**（玩家腳本，spike-web 管線）＋ **CodeMirror 6**（IDE）＋ **esbuild**（建置）。
> 完成品＝**可上傳 itch.io 的 demo**（HTML5 zip）。開發位置：`game/`。
> Side-project 節奏：以「週末」為單位估，每個里程碑結束時遊戲都能跑、都可展示。

---

## 里程碑總覽

| # | 名稱 | 完成判準（驗收） | 粗估 |
|---|---|---|---|
| M0 | 技術驗證 | ✅ 已完成（2026-07-24，`coding-punk/spike-web/`，五項全過） | — |
| M1 | 地圖可行走 | ✅ 已完成（2026-07-31）：佔位美術街區、逐格移動、鏡頭、機房入口 | — |
| M2 | 一關 dungeon | ✅ 已完成（2026-07-31）：引擎 ES module 化＋對拍全綠、IDE panel、L1 可通關 | — |
| M3 | 基礎故事 | ✅ 已完成（2026-08-01）：獨白→老周接案→通關→交差 TBC、flag＋存檔、重載接續 | — |
| M4 | 收尾＋itch.io 上架 | 10–15 分鐘完整流程，自己「假裝第一次玩」通過，itch.io 頁面上線 | 1 週末 |

順序刻意把 dungeon（M2）排在故事（M3）前：dungeon 是整合風險最高的部分（引擎移植＋直譯器管線＋IDE 接線），
故事系統反而簡單；先確定風險件活了，故事才知道要繞著什麼寫。

---

## M0 — 技術驗證 ✅（2026-07-24 完成）

spike-web 五項全過：Worker↔主執行緒 async 閘門委派、逐 statement 行號（JS-Interpreter＋Babel `retainLines`）、
雙層防呆（步數上限＋`worker.terminate()`）、CodeMirror 6 JS 高亮、vim motion 開箱即用。
細節與誠實取捨見 `coding-punk/spike-web/README.md`。**不需重做，M1 直接開工。**

## M1 — 地圖可行走 ✅（2026-07-31 完成）

0. **腳手架**：`game/` 新專案，仿 spike-web 建置模式（`build.mjs` esbuild＋`serve.mjs`＋puppeteer-core 無頭驗收）。
   目錄：`src/engine/`（grid 引擎，M2 移植）、`src/runtime/`（Worker 直譯器管線，自 spike-web 移植）、
   `src/ide/`（CodeMirror 設定）、`src/world/`（Pixi 城市場景）、`src/dungeon/`（M2）、`test/`。
   **引擎必須是純 ES module（node 可直接 import）**——回歸測試不再走 prototype 的 sloppy-eval。
1. **地圖**：一條主街＋2–3 個可進入點的小街區。先用**佔位 tileset（純色格即可）開工，不被美術卡住**；
   cyberpunk asset pack 挑選是獨立差事（itch.io 關鍵字 `cyberpunk tileset top-down`，候選收 3–5 個比風格），之後換皮。
   - AI 生成定位不變：「補洞」不是「主力」——pack 打底、AI 產大張單品（NPC 立繪、招牌海報），混用後製統一色調。
2. **Tiled JSON 載入器**：地圖用 Tiled 編（或先用字串陣列等價格式），tile layer→Pixi sprite/container；
   碰撞層＝grid 查表（**無物理引擎**，走格子遊戲用不到）。
3. **角色**：4 方向**逐格移動**（古典 RPG 式，符合本作 grid DNA）＋朝向＋走路動畫（Pixi `AnimatedSprite`；佔位素材可先用色塊＋箭頭）。
4. **鏡頭**：world container 偏移跟隨，限制在地圖邊界內。
5. **互動系統雛形**：面向格偵測可互動物（門、NPC、終端機）顯示提示，按鍵觸發；先做一個 dungeon 入口（進去暫時只切到空場景）。

**驗收**：能在街區走 1 分鐘不穿牆、不卡死，走到入口能進入另一場景再走出來。
另立無頭驗收（`npm test`，spike-web 模式）：移動 N 格、碰撞阻擋、場景切換三項自動化，exit 0＝PASS。

## M2 — 一關 dungeon（把 prototype L1 搬進來）✅（2026-07-31 完成，`npm test`＝引擎 14 案例＋瀏覽器 6 項全綠）

1. **引擎移植**：prototype grid 引擎最小集抽成 `src/engine/` 純 ES module——地圖字串解析（`#~!>s` 等）、回合 tick、
   視野（BFS ≤3＋牆擋視野）、戰鬥數值（hero 50/6、slime 10/3）、毒沼/藥水。**確定性優先，動畫其次。**
2. **API v0 接線**：14 函式照舊（詞彙不改）綁進 Worker 直譯器管線；一次行動呼叫＝一回合，
   行動經 async 閘門 yield 回主執行緒播動畫。
3. **對拍驗證**：prototype 的 JS 引擎當 reference——同一張 L1 地圖＋同一份腳本，兩邊逐回合狀態（位置/HP/回合數）必須一致；
   headless 測試中 L1 相關案例移植到新引擎（node 直接 import，不再 sloppy-eval）。
4. **Dungeon 場景視覺**（Pixi）：tile 渲染、勇者/敵人 sprite、移動/攻擊 tween、視野外調暗。
5. **IDE panel**：CodeMirror＋執行/停止/重跑按鈕＋log 區＋逐行反白（M0 成果接上）＋vim 開關；通關顯示回合數與 par ⭐。
6. 通關後回到城市場景。

**驗收**：從城市走進 dungeon，用初始腳本（逐行註解版）通過 L1，看得到逐行執行與回合動畫；改爛腳本會死、死了能重來。
**範圍護欄**：只做 L1、不做 dojo/skill、不做多關選單——那些是 v0.2。

## M3 — 基礎故事 ✅（2026-08-01 完成，驗收＝無頭走完整劇情流程＋重載接續）

1. **對話系統**：自製最小版（JSON 對話資料＋簡單打字機 UI 即可；web 生態沒有 Dialogue Manager 這種現成輪子，但需求薄、自製便宜）。
2. **劇情最小集**（一條線，無分支）：
   - 開場：主角公寓/街頭，一小段獨白建立世界觀（自由駭客、IDE 是唯一資產）。
   - 接案：1 個委託人 NPC 對話 → 取得任務「潛入 XX 機房」→ dungeon 入口解鎖。
   - 結尾：通關回來交差，拿報酬，拋一個主線鉤子（這案子背後有鬼）→「To be continued」。
3. **任務 flag**（未接/進行中/完成）＋ localStorage 存檔（位置、flag、編輯器內容；key 與 prototype 分開）。
4. 順手放 1–2 個純氛圍 NPC（一句話對話），讓街區不像鬼城。

**驗收**：新開存檔 → 對話 → 進 dungeon → 通關 → 交差，流程無死路；中途關分頁重開能接續。

## M4 — 收尾＋itch.io 上架

1. **最低限度教學**（prototype 學到的教訓，不可省；素材沿用《新手引導步驟腳本.md》）：開場引導走路/互動按鍵；
   dungeon 首次進入時 3 步提示（這是編輯器 → 按執行看看 → 改這一行再跑）；初始腳本逐行註解；死亡時給指向性提示。
2. 自己「假裝第一次玩」完整走一遍，卡住的地方就是 bug。
3. **itch.io 打包上架**：`npm run build` 產物＋`index.html` 打成 zip（資源全相對路徑、`index.html` 在 zip 根目錄）；
   itch.io 建 HTML5 專案（先 draft/restricted）、設定視窗尺寸與全螢幕按鈕、封面圖、頁面文案＋回饋表單連結；
   自己從 itch 頁面完整玩一遍（含 localStorage 存檔在 itch domain 下正常）才轉公開。
4. （可選）bundle 瘦身：Babel standalone 只留 arrow/block-scoping 等必要 plugin（worker 4.9MB 可大幅縮小）；
   錄 1 分鐘畫面當宣傳素材起點。

**驗收**：itch.io 頁面開起來，10–15 分鐘從開場玩到 To be continued。

---

## 全程護欄

- **每個里程碑結束遊戲都能跑**；做到一半的功能藏在 flag 後面，不留半殘。
- 範圍凍結：v0.1 沒有 dojo、skill、多關、解題任務、放置模式、外觀系統。想到好點子 → 記進 backlog，不現在做。
- CLAUDE.md 慣例延續：引擎改動要有對拍/測試；新決策寫回 CLAUDE.md。
- **Godot 專案檔不刪**（`coding-punk/` 根目錄的 csproj/project.godot 與 `spike/`）——M0 歷史紀錄；
  **prototype 不動**——reference implementation＋對拍基準＋現成回饋管道。

## v0.2 預告（完成 v0.1 後再議）

多關 + dojo/skill 移植 → 第一個「解題→變 plugin」任務 → itch.io 頁面公開＋社群發連結收非同步回饋。
