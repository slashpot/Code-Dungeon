# v0.1 實行步驟(第一個 Godot 版本)

> 2026-07-24。目標內容:基礎故事、基礎視覺、地圖可行走、一關 dungeon。
> 平台:Godot 4.x(.NET 版)+ C# + Jint,桌面優先。
> Side-project 節奏:以「週末」為單位估,每個里程碑結束時遊戲都能跑、都可展示。

---

## 里程碑總覽

| # | 名稱 | 完成判準(驗收) | 粗估 |
|---|---|---|---|
| M0 | 技術驗證 | Jint 跑玩家 JS + 逐行步進;CodeEdit 可編輯高亮 | 1–2 週末 |
| M1 | 地圖可行走 | 角色在有碰撞的街區走動,能進出 dungeon 入口 | 1–2 週末 |
| M2 | 一關 dungeon | 遊戲內 IDE 寫腳本通過 L1,含回合動畫與 par | 3–4 週末 |
| M3 | 基礎故事 | 開場 → NPC 對話接案 → 通關 → 結尾,劇情 flag + 存檔 | 1–2 週末 |
| M4 | 收尾打包 | 10–15 分鐘完整流程,自己「假裝第一次玩」通過,desktop build | 1 週末 |

順序刻意把 dungeon(M2)排在故事(M3)前:dungeon 是技術風險最高的部分,
故事系統反而是成熟輪子;先確定風險件活了,故事才知道要繞著什麼寫。

---

## M0 — 技術驗證(過不了就回頭換路線,不硬上)

1. 裝 Godot 4.x .NET 版,建專案 + git repo;確認 C# 能編譯執行空場景。
2. NuGet 加 Jint,console 跑通:C# 定義 `move()/attack()` 委派 → 玩家 JS 字串呼叫它們。
3. **逐行步進 spike(成敗關鍵)**:用 Jint 的 statement-level 中斷(DebugHandler/step 回呼)實作「每執行一個行動函式暫停,回報目前行號」。做一個醜 demo:左邊 TextEdit 顯示程式碼、目前行反白,右邊 log 印出動作序列。
4. `CodeEdit` node spike:JS 關鍵字高亮、行號、編輯後把內容餵給 Jint。
5. 防呆驗證:無窮迴圈(`while(true){}`)要能被中斷——Jint 的 statement 限制或 timeout 機制。

**驗收**:一個場景內,改程式碼 → 按執行 → 逐行反白 → log 出現動作 → 無窮迴圈不會凍死遊戲。
**若步進做不到**:記錄卡點,評估路線 B(QuickJS GDExtension)或降級(不逐行、只逐回合高亮)。

## M1 — 地圖可行走

1. 選 asset pack:找 cyberpunk / modern city 俯視 tileset(itch.io 關鍵字 `cyberpunk tileset top-down`;候選先收 3–5 個比風格)。角色 sprite 一組 4 方向走路即可。
   - **AI 生成定位為「補洞」不是「主力」**:tileset 需要格與格無縫拼接、風格一致,AI 目前做這個很吃力;AI 適合產大張單品(NPC 立繪、載入畫面、招牌/海報裝飾)。建議 pack 打底、AI 補氛圍件,混用時全部後製統一色調。
2. TileMapLayer 畫一個**小**街區(一條主街 + 2–3 個可進入點就夠),牆/物件加碰撞。
3. 玩家角色:CharacterBody2D,4 方向移動 + 走路動畫,Camera2D 跟隨(限制在地圖邊界內)。
4. 互動系統雛形:靠近可互動物(門、NPC、終端機)顯示提示,按鍵觸發;先做一個 dungeon 入口(進去暫時只切到空場景)。

**驗收**:能在街區走 1 分鐘不穿牆、不卡死,走到入口能進入另一場景再走出來。

## M2 — 一關 dungeon(把 prototype L1 搬進來)

1. C# 移植 grid 引擎最小集:地圖字串解析(`#~!>s` 等)、回合 tick、視野(BFS ≤3 + 牆擋視野)、戰鬥數值(hero 50/6、slime 10/3)、毒沼/藥水。**確定性優先,動畫其次。**
2. API v0 綁進 Jint(14 函式照舊,詞彙不改);一次行動呼叫 = 一回合,行動後 yield 回 Godot 播動畫。
3. **對拍驗證**:把舊 JS 引擎當 reference——同一張 L1 地圖 + 同一份腳本,兩邊逐回合狀態(位置/HP/回合數)必須一致。移植 headless 測試中 L1 相關案例到 C#(或先用 log diff 手動對拍)。
4. Dungeon 場景視覺:tile 渲染、勇者/敵人 sprite、移動/攻擊 tween、視野外調暗。
5. IDE panel:CodeEdit + 執行/停止/重跑按鈕 + log 區 + 逐行反白(M0 成果接上);通關顯示回合數與 par ⭐。
6. 通關後回到城市場景。

**驗收**:從城市走進 dungeon,用初始腳本(逐行註解版)通過 L1,看得到逐行執行與回合動畫;改爛腳本會死、死了能重來。
**範圍護欄**:只做 L1、不做 dojo/skill、不做多關選單——那些是 v0.2。

## M3 — 基礎故事

1. 對話系統:用現成 addon(建議 **Dialogue Manager**,Godot 4 生態最成熟、支援 C#)而非自製。
2. 劇情最小集(一條線,無分支):
   - 開場:主角公寓/街頭,一小段獨白建立世界觀(自由駭客、IDE 是唯一資產)。
   - 接案:1 個委託人 NPC 對話 → 取得任務「潛入 XX 機房」→ dungeon 入口解鎖。
   - 結尾:通關回來交差,拿報酬,拋一個主線鉤子(這案子背後有鬼)→「To be continued」。
3. 任務 flag(未接/進行中/完成)+ Godot 存檔(位置、flag、編輯器內容)。
4. 順手放 1–2 個純氛圍 NPC(一句話對話),讓街區不像鬼城。

**驗收**:新開存檔 → 對話 → 進 dungeon → 通關 → 交差,流程無死路;中途關遊戲重開能接續。

## M4 — 收尾打包

1. 最低限度教學(prototype 學到的教訓,不可省):開場引導走路/互動按鍵;dungeon 首次進入時 3 步提示(這是編輯器 → 按執行看看 → 改這一行再跑);初始腳本逐行註解;死亡時給指向性提示。
2. 自己「假裝第一次玩」完整走一遍,卡住的地方就是 bug。
3. 匯出 desktop build(至少自己的平台),確認 build 後 Jint/存檔/字型都正常。
4. (可選)錄 1 分鐘畫面——第一個里程碑的紀念,也是日後宣傳素材的起點。

**驗收**:雙擊 build 出來的執行檔,10–15 分鐘從開場玩到 To be continued。

---

## 全程護欄

- **每個里程碑結束遊戲都能跑**;做到一半的功能藏在 flag 後面,不留半殘。
- 範圍凍結:v0.1 沒有 dojo、skill、多關、解題任務、放置模式、外觀系統。想到好點子 → 記進 backlog,不現在做。
- CLAUDE.md 慣例延續:引擎改動要有對拍/測試;新決策(含 M0 的技術結論)寫回 CLAUDE.md。

## v0.2 預告(完成 v0.1 後再議)

多關 + dojo/skill 移植 → 第一個「解題→變 plugin」任務 → 非同步回饋管道上線(itch.io)。
